# Architecture

This monorepo contains one product with three surfaces: Rust backend, Android client, and admin web UI. The backend owns the public API and admin API. The admin UI is intended to be shipped with the backend under `/admin/*`.
