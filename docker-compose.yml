# Root compose entrypoint delegates to the backend development stack.
# The backend image build uses the repository root context so it can include
# the admin SPA from apps/admin.
include:
  - backend/docker-compose.yml
