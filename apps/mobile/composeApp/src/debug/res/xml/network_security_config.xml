<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug only: allow HTTP to local LAN development servers such as
         http://192.168.1.15:8080. Release uses the stricter androidMain config. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
