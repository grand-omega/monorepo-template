# monorepo-template

This is one product workspace containing the Rust backend, Android client, and admin web UI.

Prefer root-level docs and contracts for cross-project decisions. Keep implementation changes inside the owning subtree unless the task explicitly touches shared workflow, CI, or deployment.
