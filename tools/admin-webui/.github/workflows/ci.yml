name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-typecheck-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run openapi:gen
      - run: npm run lint
      - run: npm run format:check
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - name: Bundle budget — main chunk ≤ 250 KB gzipped
        run: |
          set -euo pipefail
          MAIN_GZ=$(find dist/assets -name 'index-*.js' -print0 | xargs -0 -I{} sh -c 'gzip -c "{}" | wc -c' | sort -nr | head -1)
          echo "main chunk gzipped: ${MAIN_GZ} bytes"
          if [ "${MAIN_GZ}" -gt 256000 ]; then
            echo "::error::main chunk exceeds 250 KB gzipped budget (${MAIN_GZ} bytes)"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          retention-days: 7

  e2e:
    runs-on: ubuntu-latest
    needs: lint-typecheck-test-build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run openapi:gen
      - run: npx playwright install --with-deps chromium
      # TODO(@yanwenxu, 2026-05-31): wire up docker-compose with backend image
      # for true contract-drift coverage. Skipping for scaffolding step.
      - run: echo "E2E against real backend wired up in step 5 (auth)."

  openapi-drift:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      # TODO(@yanwenxu, 2026-05-31): fetch live spec from a deployed backend
      # and diff against openapi.snapshot.json. Stub for now.
      - run: echo "drift check enabled in step 2 (codegen pipeline)."
