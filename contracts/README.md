# Contracts

Shared API contracts, OpenAPI snapshots, error-shape notes, and cross-client behavior documentation belong here.

Current source of truth remains the backend `/openapi.json` plus the admin UI snapshot until contract files are consolidated.
